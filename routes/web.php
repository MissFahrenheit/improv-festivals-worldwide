<?php

use Illuminate\Support\Facades\Route;

// You will need to comment this in if you want to rename the index.html to anything else
// Route::get("/", fn() => response()->file(public_path("otherFilename.html")));
